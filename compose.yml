services:
  gemini-balance:
    image: ghcr.io/snailyp/gemini-balance:latest
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped # Explicitly set, though often the default for compose